import { colors } from '../../resources/colors';
import { INPUT_FIELD_BORDER_COLOR, INPUT_FIELD_BORDER_RADIUS, INPUT_FIELD_HEIGHT } from '../constants';

export const Select = {
  baseStyle: {
    borderWidth: '1px',
    borderRadius: INPUT_FIELD_BORDER_RADIUS,
    height: INPUT_FIELD_HEIGHT,
    backgroundColor: colors.alabasterGray,
    textColor: colors.codGray,
    borderColor: INPUT_FIELD_BORDER_COLOR,
    iconColor: colors.codGray,
  },
  sizes: {},
  variants: {
  },
  defaultPros: {},
};
